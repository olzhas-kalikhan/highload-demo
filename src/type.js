/**
 * @fileoverview Global JSDoc type definitions
 */

/**
 * @callback HttpHandler
 * @param {import('express').Request} req
 * @param {import('express').Response} res
 */

// Export an empty object so Node recognizes this as a module
module.exports = {};
